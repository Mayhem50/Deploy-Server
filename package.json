{
  "name": "deploy-server",
  "version": "0.0.1",
  "description": "Github Webhook server for deploying website",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "deploy"
  ],
  "author": "B-RR",
  "license": "ISC",
  "dependencies": {
    "github-webhook-handler": "^0.6.0",
    "http": "0.0.0"
  }
}
